package com.runecast.demo.controller;

/*import com.runecast.demo.domainobject.ECInstanceDO;
import com.runecast.demo.service.ECDemoService;*/
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@RestController
public class ECInstRelController {

	/*
	 * @Autowired private ECDemoService ecDemoService;
	 */
	/*
	 * @GetMapping("/") public List<ECInstanceDO> getAllEcInstRelations() { return
	 * ecDemoService.findAllEcInstRelations(); }
	 */
}
